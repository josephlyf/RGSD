# RGSD
RGSD is a standard involute tooth profile dataset containing strictly paired static and dynamic blurred images. The images are 4320×640 pixels in size. All motion parameters, gear parameters, and optical calibration parameters are provided in parameter.txt, which have been verified by precision instruments with only minor deviations.
